# frozen_string_literal: true

# Operations Layer - Use case orchestration
# This layer contains commands (write operations) and queries (read operations)
# Orchestrates domain entities and infrastructure adapters via dependency injection
# Depends only on Domain layer for business logic
module Operations
end
