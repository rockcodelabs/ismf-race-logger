# frozen_string_literal: true

# Infrastructure Layer - External system adapters
# This layer contains adapters for persistence (ActiveRecord), external APIs, 
# file storage, background jobs, mailers, and other external concerns
# Maps between domain entities and external representations
# Depends only on Domain layer for entity definitions
module Infrastructure
end