<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - ISMF Race Logger</title>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#1a1a2e">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%# Poppins font from Google Fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full" style="font-family: 'Poppins', system-ui, sans-serif;">
    <div class="min-h-full flex">
      <!-- Sidebar -->
      <aside class="hidden md:flex md:w-64 md:flex-col md:fixed md:inset-y-0">
        <div class="flex flex-col grow pt-5 bg-ismf-navy overflow-y-auto">
          <!-- Logo -->
          <div class="flex items-center shrink-0 px-4">
            <%= link_to root_path, class: "flex items-center gap-3 hover:opacity-90 transition-opacity" do %>
              <div class="w-10 h-10 bg-ismf-red rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
              </div>
              <div>
                <span class="text-white font-semibold text-lg">ISMF</span>
                <span class="text-ismf-red font-medium text-sm block -mt-1">Race Logger</span>
              </div>
            <% end %>
          </div>
          
          <!-- Navigation -->
          <nav class="mt-8 flex-1 px-3 space-y-1">
            <%= link_to admin_root_path, class: "flex items-center gap-3 px-3 py-2.5 text-sm font-medium rounded-lg transition-colors #{request.path == admin_root_path ? 'bg-ismf-red text-white' : 'text-gray-300 hover:bg-ismf-blue hover:text-white'}" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
              </svg>
              Dashboard
            <% end %>
            
            <%= link_to admin_users_path, class: "flex items-center gap-3 px-3 py-2.5 text-sm font-medium rounded-lg transition-colors #{controller_name == 'users' ? 'bg-ismf-red text-white' : 'text-gray-300 hover:bg-ismf-blue hover:text-white'}" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z" />
              </svg>
              Users
            <% end %>
          </nav>
          
          <!-- User info at bottom -->
          <div class="shrink-0 flex border-t border-ismf-blue p-4">
            <div class="flex items-center w-full">
              <div class="shrink-0">
                <div class="w-9 h-9 bg-ismf-red rounded-full flex items-center justify-center">
                  <span class="text-white font-medium text-sm">
                    <%= Current.user&.name&.first&.upcase || Current.user&.email_address&.first&.upcase || "?" %>
                  </span>
                </div>
              </div>
              <div class="ml-3 flex-1 min-w-0">
                <p class="text-sm font-medium text-white truncate">
                  <%= Current.user&.display_name %>
                </p>
                <p class="text-xs text-gray-400 truncate">
                  <%= Current.user&.admin? ? "Administrator" : "User" %>
                </p>
              </div>
              <%= button_to session_path, method: :delete, class: "ml-2 p-1.5 text-gray-400 hover:text-white transition-colors", data: { turbo_confirm: "Are you sure you want to sign out?" } do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                </svg>
              <% end %>
            </div>
          </div>
        </div>
      </aside>

      <!-- Main content -->
      <div class="flex-1 flex flex-col md:pl-64">
        <!-- Mobile header -->
        <header class="md:hidden bg-ismf-navy">
          <div class="flex items-center justify-between px-4 py-3">
            <div class="flex items-center gap-2">
              <div class="w-8 h-8 bg-ismf-red rounded-lg flex items-center justify-center">
                <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
                </svg>
              </div>
              <span class="text-white font-semibold">ISMF Race Logger</span>
            </div>
            <%= button_to session_path, method: :delete, class: "p-2 text-gray-300 hover:text-white", data: { turbo_confirm: "Sign out?" } do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
              </svg>
            <% end %>
          </div>
          <!-- Mobile navigation -->
          <nav class="flex gap-1 px-3 pb-3 overflow-x-auto">
            <%= link_to "Dashboard", admin_root_path, class: "shrink-0 px-3 py-1.5 text-sm font-medium rounded-full transition-colors #{request.path == admin_root_path ? 'bg-ismf-red text-white' : 'text-gray-300 hover:bg-ismf-blue'}" %>
            <%= link_to "Users", admin_users_path, class: "shrink-0 px-3 py-1.5 text-sm font-medium rounded-full transition-colors #{controller_name == 'users' ? 'bg-ismf-red text-white' : 'text-gray-300 hover:bg-ismf-blue'}" %>
          </nav>
        </header>

        <!-- Page content -->
        <main class="flex-1 overflow-y-auto">
          <div class="py-6">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
              <!-- Flash messages -->
              <% if notice %>
                <div class="flash-notice mb-6">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <%= notice %>
                </div>
              <% end %>
              
              <% if alert %>
                <div class="flash-alert mb-6">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <%= alert %>
                </div>
              <% end %>
              
              <%= yield %>
            </div>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
